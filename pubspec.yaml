name: asgnmnt
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^11.0.2
  intl: ^0.20.2
  path_provider: ^2.1.5
  supabase_flutter: ^2.12.4
  url_launcher: ^6.3.1
  fl_chart: ^0.68.0
  path_provider: ^2.1.2
  share_plus: ^10.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true